{
  "private": true,
  "name": "backend",
  "main": "src/index.ts",
  "scripts": {
    "dev": "env-cmd -f .env ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "env-cmd -f .env ts-node --transpile-only src/index.ts",
    "db:sync": "env-cmd -f .env ts-node --transpile-only src/scripts/db-sync.ts",
    "init-db": "env-cmd -f .env ts-node --transpile-only src/scripts/init-db.ts"
  },
  "dependencies": {
    "@types/sequelize": "^4.28.20",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.14.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "env-cmd": "^10.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
